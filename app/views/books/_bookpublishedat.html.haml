- publishedats = fillResponseArrays(@doc, @highlight, 'published_at', @id)
- more = @doc['published_at'].length - publishedats.length
- publishedats.each do |publishat|
  &bull;
  = publishat.kind_of?(Array) ? publishat[0] : publishat
- if (more > 0)
  = I18n.t(:and_count_more, :count => more)
