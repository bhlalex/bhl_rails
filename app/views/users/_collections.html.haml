.page
  .page_border
    - if (@user_collections != '' && @user_collections_total_number > 0)
      .count
        = "Found #{pluralize(@user_collections_total_number, I18n.t(:collection))} "
      - @user_collections.each do |collection|
        = render :partial => "collections/collection", :locals => {:with_rate => 2, :collection => collection}
      - if(!((@page == @lastPage && @lastPage == 1) || @lastPage == 0))
        - @pages = adjustPaging(@page, @lastPage)
        = render :partial => "books/paging"
    - else
      = I18n.t(:no_collections_found)