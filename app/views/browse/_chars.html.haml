- if @char == ''
  %b
    = I18n.t(:browse_all)
- else
  = link_to "all" do
    = I18n.t(:browse_all)
- @alphas.each do |alpha|
  | 
  - if @char.downcase == alpha.downcase
    %b
      = alpha
  - else
    = link_to "#{alpha.downcase}" do
      = alpha